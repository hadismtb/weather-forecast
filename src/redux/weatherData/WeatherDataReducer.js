const initialState = {
    data : [],
    loading: false,
    error : ""
}

const WeatherDataReducer = (state = initialState, action) => {
    const { type, payload } = action

    switch (type) {
        case ""
    }
}